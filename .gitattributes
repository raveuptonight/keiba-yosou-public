# =============================================================================
# Git LFS Configuration for keiba-yosou
# Tracks large binary files (ML models) with Git LFS
# This allows the repository to be made public without exposing model weights
#
# Setup: git lfs install
# Migrate existing files: git lfs migrate import --include="*.pkl"

# ML Model files (pickle format)
models/*.pkl filter=lfs diff=lfs merge=lfs -text
models/backup/*.pkl filter=lfs diff=lfs merge=lfs -text
*.pkl filter=lfs diff=lfs merge=lfs -text
# Other large binary formats (future-proofing)
*.h5 filter=lfs diff=lfs merge=lfs -text
*.pt filter=lfs diff=lfs merge=lfs -text
*.pth filter=lfs diff=lfs merge=lfs -text
*.onnx filter=lfs diff=lfs merge=lfs -text
*.joblib filter=lfs diff=lfs merge=lfs -text
# Large data files
*.parquet filter=lfs diff=lfs merge=lfs -text
*.feather filter=lfs diff=lfs merge=lfs -text
